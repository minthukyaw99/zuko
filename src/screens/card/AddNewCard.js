import React from 'react';
import { View, Text } from 'react-native';

const AddNewCard = () => (
    <View>
        <Text>
            Add New Card
        </Text>
    </View>
);

export default AddNewCard;
